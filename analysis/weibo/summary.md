# weibo 图结构分析

输入：`D:\研究生\研究\2026参考代码\GRID_Ericxie\dataset\origin_data\Weibo\graph.txt`。
分析对象是去掉特殊节点、自环、重复边后的无权社交图，不是传播树。
方向处理：有向；模式 auto。

## 树状结构（无向投影）

- 节点 31061，边 293119，连通分量 3，孤立节点 0。
- 是否为树：False；是否为森林：False。
- 独立环数量 m−n+c：262061；变为森林至少需移除 89.40% 的边。
- 桥 1419 条；叶节点 1415 个；非孤立树组件 2 个。
环冗余越低、桥占比越高，通常越接近森林，但没有统一的“树状”分类阈值。

## 层级特征

- 最大核数：47（核心/外围结构指标）。
- 最大连通分量中，从最高度节点出发的最远距离：4。
- 聚类系数使用 1000 个节点；C(k) 趋势仅作描述。
- 最大强连通分量覆盖 72.67% 的节点；压缩图最长路径为 9 条边。
强连通分量内部存在可互达结构；压缩图必为 DAG，不能据此宣称原图具有严格层级。
BFS 层次依赖根节点；这些结构指标不证明传播方向或因果关系。

## 幂律特征

- undirected：alpha=2.459，xmin=24，尾部 n=4529，KS=0.0140，bootstrap p=0.017982017982017984；在 0.1 阈值拒绝幂律尾部。
- in_degree：alpha=2.411，xmin=19，尾部 n=2528，KS=0.0107，bootstrap p=0.42957042957042957；未拒绝幂律尾部，但不等于证明幂律。
- out_degree：alpha=2.448，xmin=13，尾部 n=4484，KS=0.0098，bootstrap p=0.26673326673326675；未拒绝幂律尾部，但不等于证明幂律。

零度节点不参与幂律尾部拟合，其数量另行报告。阈值采用候选搜索中的最小 KS，候选数量见 JSON。
指数分布比较仅是一个替代模型，未排除对数正态等解释。网络节点度数相关，bootstrap 和似然比的 iid 假设是限制。
本次 bootstrap 请求 1000 次；默认 1000 次，可用 --bootstrap 0 跳过。接近 0.1 阈值时应增加重复次数并检查蒙特卡洛误差。

参考：

- https://arxiv.org/abs/0706.1062
- https://arxiv.org/abs/cond-mat/0206130
